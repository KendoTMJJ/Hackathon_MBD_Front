import { ZoneNode } from "./ZoneNode";

export const zoneTypes = {
  zone: ZoneNode,
  "zone-internet": ZoneNode,
  "zone-dmz": ZoneNode,
  "zone-lan": ZoneNode,
  "zone-datacenter": ZoneNode,
  "zone-cloud": ZoneNode,
  "zone-ot": ZoneNode,
};

export { ZoneNode };
